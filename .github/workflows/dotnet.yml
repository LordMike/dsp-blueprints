name: Generic Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@0717577d45739eb3c851188b29f50ed6c0b2194e # v2
      with:
        fetch-depth: 50
    - run: git fetch --tags

    - name: Setup .NET
      uses: actions/setup-dotnet@871f041373faaad213a635d9afb62905ec029bbb # v1
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Test
      run: "[ -f .ci-skip-tests ] && echo 'Skipping tests' || dotnet test --no-build --verbosity normal"

    - name: Pack
      if: github.ref == 'refs/heads/master'
      run: dotnet pack -c Debug -o Build

    - name: "Github packages: Nuget push"
      if: github.ref == 'refs/heads/master'
      run: dotnet nuget push --no-symbols --skip-duplicate -k ${{ secrets.GITHUB_TOKEN }} -s "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" Build/*.nupkg
      
    - name: "Github packages: Cleanup"
      if: github.ref == 'refs/heads/master'
      uses: stripethree/gpr-janitor@f3704c21869d8e021ba14e340ea28c879b35d1b1 # dist
      with:
        dry-run: false
        keep-versions: 25
        min-age-days: 14
        versions-to-fetch: 20
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
